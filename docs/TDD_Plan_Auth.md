# TDD Plan: KIS API 인증 모듈

## 1. 토큰 발급 (완료)
- `get_access_token()` 호출 시 토큰이 없으면 신규 발급.

## 2. 토큰 재사용 (완료)
- 유효한 토큰 캐싱 및 API 중복 호출 방지.

## 3. 토큰 자동 갱신 (완료)
- 토큰 만료 시 신규 발급.

## 4. 토큰 폐기 (완료)
- `revoke_token()` 정상 작동.

## 5. 실시간 웹소켓 접속키 발급 (완료)
- `get_approval_key()` 정상 작동.

## 6. Rate Limit 준수 (완료)
- 호출 간 최소 0.2초 간격 유지 확인.
